
pipeline {
  agent {
      label 'master'
  }
  environment {
    TOKEN = credentials('ghe-token')
  }
  stages {
    stage('BLAH') {
      steps {
        script {
          deploymentId = githubCreateDeployment()
          echo deploymentId
        }
      }
    }
  }
}
