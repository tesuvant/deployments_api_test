
pipeline {
  agent {
      label 'master'
  }
  environment {
    GITHUB_OAUTH = credentials('ghe-token')
  }
  stages {
    stage('BLAH') {
      steps {
        script {
          deploymentId = githubCreateDeployment("deployments_api_test")
          echo deploymentId
        }
      }
    }
  }
}
