
pipeline {
  agent {
      label 'master'
  }
  environment {
    GITHUB_OAUTH = credentials('')
  }
  stages {
    stage('clone') {
      steps {
        script {
          deploymentId = githubCreateDeployment("deployments_api_test")
          echo deploymentId
        }
      }
    }
  }
}
