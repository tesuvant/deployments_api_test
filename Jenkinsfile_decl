
pipeline {
  agent {
      label 'master'
  }
  environment {
    GITHUB_OAUTH = credentials('ghe-token')
  }
  stages {
    stage('BLAH') {
      steps {
        script {
          deploymentId = githubCreateDeployment()
          echo deploymentId
        }
      }
    }
  }
}
